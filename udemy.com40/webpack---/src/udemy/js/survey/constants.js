// eslint-disable-next-line import/prefer-default-export
export const MAX_SELECTABLE_CHECKBOXES = 3;

// Main question types for surveys
export const QUESTION_TYPE = Object.freeze({
    FREE_FORM: 'freeform',
    CHOICE: 'choice',
});
