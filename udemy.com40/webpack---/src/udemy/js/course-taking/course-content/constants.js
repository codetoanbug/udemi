export const STATUS_TOGGLE_OPTIONS = Object.freeze({
    ENABLED: 'enabled',
    DISABLED: 'disabled',
    HIDDEN: 'hidden',
});

export const TRACKING_ACTIONS = Object.freeze({
    VISIT: 'visit',
});

export const MAX_VISIBLE_CUES = 5;
