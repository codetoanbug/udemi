import {MainContentLoader} from '@udemy/react-reveal-components';
import React from 'react';

import './loader.less';

const Loader = (props) => <MainContentLoader styleName="loader" {...props} />;

export default Loader;
