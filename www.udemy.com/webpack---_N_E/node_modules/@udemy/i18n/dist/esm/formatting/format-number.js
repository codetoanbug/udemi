export function formatNumber(value, fullLocale) {
  let options =
    arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
  const localeTokens = fullLocale.split("_");
  const locale = localeTokens.length > 0 ? localeTokens[0] : "en";
  return Number(value).toLocaleString(locale, options);
}
export function roundNumber(number) {
  let precision =
    arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;
  const power = Math.pow(10, precision);
  return Math.round(number * power) / power;
}
export function formatRoundNumber(value) {
  let precision =
    arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;
  let fullLocale = arguments.length > 2 ? arguments[2] : undefined;
  let options =
    arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : {};
  return formatNumber(
    roundNumber(Number(value), precision).toFixed(precision),
    fullLocale,
    options
  );
}
//# sourceMappingURL=format-number.js.map
