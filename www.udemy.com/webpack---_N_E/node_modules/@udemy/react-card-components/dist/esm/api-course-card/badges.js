import React from "react";

/**
 * Converts Course API badges to `CourseCard` badges props
 *
 * @remarks
 *
 * @see `browse/types/course`
 */
export const badgesPropsFrom = (_ref) => {
  let { course, badges, children } = _ref;
  const badgeTypes = course.badges
    ? course.badges.map((badge) => badge.badge_family)
    : [];
  const Badge =
    badges === null || badges === void 0 ? void 0 : badges(badgeTypes);
  const content = /*#__PURE__*/ React.createElement(
    React.Fragment,
    null,
    Badge && /*#__PURE__*/ React.createElement(Badge, null),
    children
  );
  return {
    children: content,
  };
};
//# sourceMappingURL=badges.js.map
