import React from "react";
import styles from "./course-card-badges.module.css";
/**
 * Props for `CourseCardBadges`
 */

/**
 * Badges container for `CourseCard`
 *
 * Generally, include one {@link @udemy/react-messaging-components#Badge | `Badge`}
 * component as a child component.
 *
 * It will not render anything if there are no `children`.
 */
export const CourseCardBadges = (_ref) => {
  let { children } = _ref;
  return children
    ? /*#__PURE__*/ React.createElement(
        "div",
        {
          className: styles["course-badges"],
        },
        children
      )
    : null;
};
//# sourceMappingURL=course-card-badges.react-component.js.map
