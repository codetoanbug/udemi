import { useI18n } from "@udemy/i18n";
import classNames from "classnames";
import React from "react";
import styles from "./meter.module.css";
/** React props interface for the `Meter` component */

/**
 * The Meter component is modeled after the HTML5 <meter /> tag:
 * https://developer.mozilla.org/en-US/docs/Web/HTML/Element/meter
 *
 * @privateRemarks
 *
 * We are not actually rendering a <meter /> HTML tag because it is
 * difficult to style.
 */
export const Meter = (_ref) => {
  let { value, min, max, label, className, ...htmlProps } = _ref;
  const { interpolate } = useI18n();
  const percent = Math.round(((value - min) / (max - min)) * 100);
  return /*#__PURE__*/ React.createElement(
    "div",
    Object.assign({}, htmlProps, {
      className: classNames(
        className,
        "ud-meter-wrapper",
        styles["meter-wrapper"]
      ),
    }),
    /*#__PURE__*/ React.createElement("div", {
      className: classNames("ud-meter", styles.meter),
      style: {
        transform: `scaleX(${percent / 100})`,
      },
      "aria-label": interpolate(
        label,
        {
          value,
          min,
          max,
          percent,
        },
        true
      ),
      "data-purpose": "meter",
    })
  );
};
//# sourceMappingURL=meter.react-component.js.map
