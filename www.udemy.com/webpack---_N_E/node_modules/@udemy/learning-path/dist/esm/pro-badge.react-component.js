import React from "react";
import { useI18n } from "@udemy/i18n";
import RatingStarIcon from "@udemy/icons/dist/rating-star.ud-icon";
import { Badge } from "@udemy/react-messaging-components";
import { useUDData } from "@udemy/ud-data";
import styles from "./pro-badge.module.css";
export const ProBadge = (_ref) => {
  let { className = "", background = "neutral", ...otherProps } = _ref;
  const { gettext } = useI18n();
  const udData = useUDData();
  if (udData.Config.brand.has_organization) {
    return /*#__PURE__*/ React.createElement(
      Badge,
      Object.assign(
        {
          className: `${className} ${styles["pro-badge"]}`,
        },
        otherProps
      ),
      /*#__PURE__*/ React.createElement(RatingStarIcon, {
        className: "icon",
        size: "xsmall",
        label: false,
        color: "inherit",
      }),
      "Pro"
    );
  }
  return /*#__PURE__*/ React.createElement(
    Badge,
    Object.assign(
      {
        className: `${className} ${styles[background]}`,
      },
      otherProps
    ),
    gettext("Beta")
  );
};
ProBadge.displayName = "ProBadge";
//# sourceMappingURL=pro-badge.react-component.js.map
