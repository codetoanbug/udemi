// Convert Px to Rem
export const pxToRem = (px) => px / 10; // Convert Px to Em

export const pxToEm = (px) => Math.round((px / 16) * 100) / 100; // Convert Em to Px, in cases where reverse conversion is needed

export const emToPx = (em) => {
  // if string, we assume a string ending with `em`
  const emValue = typeof em === "string" ? Number(em.replace("em", "")) : em;
  return Math.round(((emValue * 16) / 100) * 100);
};
//# sourceMappingURL=index.js.map
