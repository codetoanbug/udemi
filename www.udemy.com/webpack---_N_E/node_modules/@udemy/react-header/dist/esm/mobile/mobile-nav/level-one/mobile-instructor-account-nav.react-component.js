import { observer } from "mobx-react";
import React from "react";
import { useI18n } from "@udemy/i18n";
import { useUDData } from "@udemy/ud-data";
import { useHeaderStore } from "../../../hooks/use-header-store";
import {
  MobileNavItem,
  MobileNavL1Nav,
  MobileNavSection,
} from "../mobile-nav.react-component";
export const MobileInstructorAccountNav = observer(() => {
  const headerStore = useHeaderStore();
  const { instructorURLs } = headerStore;
  const { Config } = useUDData();
  const udConfig = Config;
  const { gettext } = useI18n();
  return /*#__PURE__*/ React.createElement(
    MobileNavL1Nav,
    {
      id: "header-toggle-side-nav-instructor-account",
    },
    /*#__PURE__*/ React.createElement(
      MobileNavSection,
      null,
      udConfig.brand.is_profile_functions_enabled &&
        /*#__PURE__*/ React.createElement(
          MobileNavItem,
          {
            href: instructorURLs.ACCOUNT,
          },
          gettext("Account security")
        ),
      /*#__PURE__*/ React.createElement(
        MobileNavItem,
        {
          href: instructorURLs.EDIT_NOTIFICATIONS,
        },
        gettext("Notification settings")
      ),
      !udConfig.brand.has_organization &&
        /*#__PURE__*/ React.createElement(
          MobileNavItem,
          {
            href: instructorURLs.API_CLIENTS,
          },
          gettext("API clients")
        ),
      !udConfig.brand.has_organization &&
        /*#__PURE__*/ React.createElement(
          MobileNavItem,
          {
            href: instructorURLs.CLOSE_ACCOUNT,
          },
          gettext("Close account")
        )
    )
  );
});
//# sourceMappingURL=mobile-instructor-account-nav.react-component.js.map
