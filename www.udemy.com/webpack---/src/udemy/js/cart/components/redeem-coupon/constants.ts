export const MODES = {
    DISPLAY: 'display',
    INPUT: 'input',
    OFF: 'off',
    PENDING: 'pending',
};

export type ModesKeys = keyof typeof MODES;

// eslint-disable-next-line import/no-default-export
export default MODES;
